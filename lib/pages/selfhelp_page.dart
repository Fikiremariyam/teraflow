import 'package:flutter/material.dart';

class SelfHelpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Self-Help")),
      body: Center(
        child: Text('Welcome to the Self-Help Page!'),
      ),
    );
  }
}
