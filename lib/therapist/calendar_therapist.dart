import 'package:flutter/material.dart';

class CalendarTherapist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome to the Calendar Page!'),
      ),
    );
  }
}
