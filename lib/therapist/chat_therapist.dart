import 'package:flutter/material.dart';

class ChatTherapist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome to the Chat Page!'),
      ),
    );
  }
}
